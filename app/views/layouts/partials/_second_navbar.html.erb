<nav class="navbar">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'COMPANY LOGO', root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="#">DASHBOARD</a></li>
        <% if user_signed_in? %>
          <li class="<%= active_class(profile_path(current_user.profile)) %>">
            <%= link_to 'MY PROFILE', profile_path(current_user.profile) %>
          </li>
          <li class="<%= active_class(profiles_path) %>">
            <%= link_to 'FIND A MENTOR', profiles_path %>
          </li>
        <% end %>
        <li><a href="#">FIND A MENTEE</a></li>
        <li><a href="#">RELATIONSHIPS</a></li>
        <li><a href="#">RESOURCES</a></li>

      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
